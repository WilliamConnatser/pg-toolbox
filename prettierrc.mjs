const config = {
  printWidth: 80, // Line length that the printer will wrap on
  tabWidth: 2, // Number of spaces per indentation level
  useTabs: false, // Indent lines with spaces, not tabs
  semi: false, // Do not use semicolons
  singleQuote: true, // Use single quotes instead of double quotes
  trailingComma: "all", // Trailing commas wherever possible (including function parameters)
  bracketSpacing: true, // Print spaces between brackets in object literals
  arrowParens: "always", // Always include parens for arrow functions
  endOfLine: "lf", // Line endings use line feed only (unix-style)
  proseWrap: "preserve", // Do not wrap markdown text
  quoteProps: "as-needed", // Only add quotes around object properties where required
  jsxSingleQuote: true, // Use single quotes in JSX
};

export default config;
